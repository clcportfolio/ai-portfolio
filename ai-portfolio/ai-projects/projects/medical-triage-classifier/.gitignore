# Dataset (raw + processed) — stored in S3, not committed
data/

# MLflow local runs
mlruns/
mlartifacts/
*.db

# Model artifacts
model_artifacts/
*.pt
*.bin
*.safetensors

# Standard Python
__pycache__/
*.py[cod]
.venv/
.env
